class FileSystemRetrieval:
   'Information Retrieval System using local file system as database'

   def __init__(self, retrieval_model):
      self.retrieval_model = retrieval_model

   def set_directory(self, path):
      'Change the current directory and index the documents inside'
      pass

   def query_current_dir(self, keywords):
      'TODO'
      pass